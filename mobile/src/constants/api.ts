// Expo web (browser): localhost works because both run on the same machine.
// Physical device / Android emulator: replace with your dev machine's LAN IP
//   e.g. http://192.168.2.14:8000
export const API_BASE = "http://localhost:8000"
